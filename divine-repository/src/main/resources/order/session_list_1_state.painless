double session_score = 0;
double state = doc['state'].value;
if ( state == 4 ) {
    session_score = 4;
} else if ( state == 2 ) {
   session_score = 3;
} else if ( state == 1 ) {
    session_score = 2;
} else if ( state == 3 ){
    session_score = 1;
}
return session_score;